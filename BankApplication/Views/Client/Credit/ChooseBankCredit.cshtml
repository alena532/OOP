@model IEnumerable<BankApplication.Models.Banking>
    <form asp-action="Credit" method="get">
      <select  name="BankId">
        @foreach (var bnk in @Model)
         {
         <option value=@bnk.Id>@bnk.Name</option>
         }
     </select>
     <button>OK</button>
   </form>